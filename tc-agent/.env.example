# Teach Charlie Desktop — Environment Variables
# Copy to .env and fill in values

# Clerk publishable key (same as the web app)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# API URL (defaults to production if not set)
VITE_TC_API_URL=https://app.teachcharlie.ai
